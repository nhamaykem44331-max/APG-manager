// APG Manager RMS - Booking detail layout (pass-through, parent bookings/layout has sidebar)
export default function BookingDetailLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
