// APG Manager RMS - New booking layout (pass-through, parent bookings/layout has sidebar)
export default function NewBookingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
