{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NEXTAUTH_URL", "NEXTAUTH_SECRET", "NEXT_PUBLIC_API_URL"],
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "type-check": {
      "dependsOn": ["^type-check"]
    }
  }
}
